# 生产/旧 Linux 部署用的基础依赖。
# rembg / onnxruntime / playwright 由 deploy.sh 按系统兼容性单独安装，
# 避免 CentOS 7 在基础安装阶段因 greenlet / glibc 问题直接失败。
beautifulsoup4
certifi
lxml
# Pillow 12+ 不再提供 CentOS 7（manylinux2014）wheel，会退回旧 GCC 源码编译并失败。
Pillow<12
watchdog
protobuf
psd-tools
